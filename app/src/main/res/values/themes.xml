<resources xmlns:tools="http://schemas.android.com/tools">

    <style name="Theme.StudyBuddyApp" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="colorPrimary">@color/azure</item>

        <!-- Color for the pointer -->
        <item name="colorSecondary">@color/azure</item>

        <item name="android:statusBarColor">@color/black</item>
    </style>

    <style name="Theme.StudyBuddyApp.NoStatusBar" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="colorPrimary">@color/azure</item>

        <!-- Color for the pointer -->
        <item name="colorSecondary">@color/azure</item>

        <item name="android:statusBarColor">@color/white</item>
    </style>

    <style name="Theme.StudyBuddyApp2" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/azure</item>

        <!-- Color for the pointer -->
        <item name="colorSecondary">@color/azure</item>

        <item name="android:statusBarColor">@color/black</item>
    </style>

    <style name="AlertDialogTheme" parent="Theme.AppCompat.Light.Dialog.Alert">
        <item name="android:windowAnimationStyle">@android:style/Animation.Dialog</item>
    </style>
</resources>